{{- /*
  Custom RSS output. Hugo's built-in template hardcodes the channel
  description to "Recent content on <site title>"; this uses the real site
  description instead, and carries each post's hand-written `summary` rather
  than an auto-truncated excerpt.

  Applies to the home feed and every section/taxonomy feed.
*/ -}}
{{- $pages := .RegularPages -}}
{{- if .IsHome }}{{ $pages = site.RegularPages }}{{ end -}}
{{- $limit := 30 -}}
{{- if gt (len $pages) $limit }}{{ $pages = first $limit $pages }}{{ end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if .IsHome }}{{ site.Title }}{{ else }}{{ .Title }} · {{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ if .IsHome }}{{ site.Params.description }}{{ else }}{{ with .Params.summary }}{{ . }}{{ else }}{{ site.Params.description }}{{ end }}{{ end }}</description>
    <generator>Hugo</generator>
    <language>{{ site.Language.Locale | default "en-us" }}</language>
    <managingEditor>{{ site.Params.author }}</managingEditor>
    <webMaster>{{ site.Params.author }}</webMaster>
    <copyright>{{ now.Year }} {{ site.Params.author }}</copyright>
    {{- with (index $pages 0) }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{ with .OutputFormats.Get "rss" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q/>" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{ range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      <description>{{ with .Params.summary }}{{ . }}{{ else }}{{ .Summary }}{{ end }}</description>
      {{- with .Params.categories }}{{ range . }}
      <category>{{ . }}</category>
      {{- end }}{{ end }}
    </item>
    {{ end }}
  </channel>
</rss>
